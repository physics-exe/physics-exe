[project]
name = "physics-exe"
version = "0.1.0"
description = "physics.exe Hackathon Codebase"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "plotly",
    "kaleido",
    "torch",
]
